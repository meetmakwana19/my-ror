<h1>AdminUsers#new</h1>
<p>Find me in app/views/admin_users/new.html.erb</p>

<% @page_title = "New Admin User" %>

<%= link_to("<< Back to list", admin_users_path, :class => 'back-link')%>

<div class="admin-users new">
    <h2>Create Admin User</h2>

    <%= form_for(@admin_users) do |f| %>

        <%= render(:partial => 'form', :locals => {:f => f}) %>

        <div class="form-button">
            <%= f.submit("Create Admin User") %>
        </div>

    <% end %>
</div>