<!DOCTYPE html>
<html lang="en">
<head>
    <title>Sample CMS <%= @page_title || "Admin area" %></title>
</head>
<body>

    <% if !flash[:notice].blank?%>
    <%# if notice is not blank then %>
    <div class="notice">
        <%= flash[:notice] %>
    </div>
    <% end %>

    <%# before yield %>
    <!-- before yield -->
    <%= yield %>
    <!-- after yield -->
    <%# after yield %>
</body>
</html>